# Curated structured layer -- hand-authored, not generated by extract_pack.py.
# Generation 1 (see SCHEMA.md): mechanically converted from JSON to YAML,
# content unchanged and verified equal by loading both.
pack: "rbm"
files:
  - file_kind: "network topology input"
    statement: "check the network's topology, outlet, reach/node identifiers and the ordering expected by the input reader; matching latitude/longitude alone is insufficient at confluences"
    source: null
    scope: "unspecified in the pack"
    basis: "unverified"
    from:
      file: "execution.md"
      anchor: "recommended-preflight"
inputs:
  - variable: "discharge, meteorological/heat inputs and boundary temperatures"
    aspect: "identity"
    statement: "the tutorial separates hydrological forcing, routing/network inputs and temperature-model inputs; flow direction and routing response information establish network transport; the thermal setup also requires headwater-temperature and hydraulic relationships"
    source: "R2"
    scope: "UW-Hydro VIC-RBM tutorial, retrieved 2026-09-16"
    basis: "source_read"
    from:
      file: "execution.md"
      anchor: "establish-the-implementation-first"
outputs:
  - variable: "stream temperature, headwater temperature, air temperature (network output fields)"
    aspect: "identity"
    statement: "the historical Salmon example's output description includes reach, node and segment identifiers; it distinguishes simulated stream temperature from headwater and air temperature fields"
    source: "R3"
    scope: "UW-Hydro RBM Salmon River run guide, retrieved 2026-09-16"
    basis: "source_read"
    from:
      file: "outputs.md"
      anchor: "keep-producers-separate"
  - variable: "network output record (confluence cells)"
    aspect: "identity"
    statement: "confluence cells can contain multiple nodes, so coordinates alone are not a unique record key"
    source: "R3"
    scope: "UW-Hydro RBM Salmon River run guide, retrieved 2026-09-16"
    basis: "source_read"
    from:
      file: "outputs.md"
      anchor: "keep-producers-separate"
