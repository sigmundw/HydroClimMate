# Curated structured layer -- hand-authored, not generated by extract_pack.py.
# Generation 1 (see SCHEMA.md): mechanically converted from JSON to YAML,
# content unchanged and verified equal by loading both.
pack: "wrf-urban"
workflows:
  - name: "recommended-staged-checks"
    steps:
      - step: 1
        what_is_done: "inspect the prepared domain and meteorological intervals, projection and nested-domain coverage"
        must_establish: "a small diagnostic of the urban mask/fraction is plotted if it resolves uncertainty"
        check: null
        source: null
        scope: "unspecified in the pack"
        basis: "unverified"
        from:
          file: "execution.md"
          anchor: "recommended-staged-checks"
      - step: 2
        what_is_done: "check initialization logs and expected initial/boundary files before atmospheric integration"
        must_establish: "passing preprocessing is not treated as establishing successful initialization"
        check: null
        source: null
        scope: "unspecified in the pack"
        basis: "unverified"
        from:
          file: "execution.md"
          anchor: "recommended-staged-checks"
      - step: 3
        what_is_done: "start a short authorized integration with the agreed physics and output requests"
        must_establish: "elapsed model time, log status, readable output, domain and obvious invalid values are checked"
        check: null
        source: null
        scope: "unspecified in the pack"
        basis: "unverified"
        from:
          file: "execution.md"
          anchor: "recommended-staged-checks"
      - step: 4
        what_is_done: "for continuation, inspect matching restart records, domain configuration, boundary coverage and existing jobs"
        must_establish: "a directory containing another run is not appended to blindly"
        check: null
        source: null
        scope: "unspecified in the pack"
        basis: "unverified"
        from:
          file: "execution.md"
          anchor: "recommended-staged-checks"
