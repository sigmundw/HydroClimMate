# Curated structured layer -- hand-authored, not generated by extract_pack.py.
# Re-sourced to cite the pack's own prose
# (card.md/processes.md/failures.md/recipes.md/version.md/sources.md), not the
# redirect stubs (overview.md/outputs.md/execution.md/pitfalls.md).
pack: "hrldas-noahmp"
generation: 2
generation_note: "Full reference implementation: card + process map + symptom-indexed failures + bounded recipes + version anchor, backed by catalogs/*.yaml mechanically extracted from the pinned source and confronted with real output. The other seven packs are generation 1 (see SCHEMA.md, models/index.md)."
version_scope: "source commit d9f5b205; noahmp submodule 9fbe6724 (v3.7.1-355-g9fbe672); drivers/hrldas build path"
layers: {"narrative": {"file": null, "coverage": "partial"}, "interface": {"file": "interface.yaml", "coverage": "partial"}, "switches": {"file": "switches.yaml", "coverage": "partial"}, "pitfalls": {"file": "pitfalls.yaml", "coverage": "partial"}, "workflows": {"file": "workflows.yaml", "coverage": "partial"}, "index": {"file": "index.json", "coverage": "full"}, "selftest": {"file": "selftest.yaml", "coverage": "partial"}, "catalogs": {"file": "catalogs/MANIFEST.json", "coverage": "partial"}}
notes: "Generation 2. Curated layers (interface/switches/pitfalls/workflows) are hand-authored and sparse by design: only what card.md/processes.md/failures.md/recipes.md/version.md actually state, or what a generated catalogs/*.yaml entry already carries (in which case the curated layer references the catalog instead of restating the fact). switches.yaml is 'partial' because its expected_scaling_variable fact is basis: unverified (the numerical shape of the FRC_URB2D scaling was not established from source, only that the weighting formula uses it linearly); catalogs coverage is 'partial' because the extractors' own known gaps (see catalogs/VALIDATION.md) are real, not a placeholder. index.json is generated (tools/build_index.py); it carries no claim of its own, so its coverage is 'full' by construction. Two curated overlays sit alongside the generated catalogs -- catalogs/kinds_overlay.yaml (true_kind for the variables the mechanical classifier could only call state_or_instantaneous/undetermined) and catalogs/options_overlay.yaml (README-vs-code cross-checks for physics_options.yaml) -- and three generator-level mislabels were fixed (SOILENERGY/SNOWENERGY kind, integer-field fill_note, catalogs/setup.yaml coverage/duplication); see SCHEMA.md."
